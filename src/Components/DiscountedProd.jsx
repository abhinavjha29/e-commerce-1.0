import React from "react";
import style from "./DiscountProd.module.css";
import { NavLink } from "react-router-dom";

const DiscountedProd = ({ name, orignialPrice, discountedPrice, image }) => {
  return (
    <div>
    
    </div>
  );
};

export default DiscountedProd;
